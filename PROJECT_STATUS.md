# Project Status

## Completed

- **Smart Search in AppHeader**: searches assets (WHITELIST_SYMBOLS with bias), education articles/tips, and page shortcuts. Dropdown with keyboard navigation, grouped results, click-outside close. Education page handles `?article=ID` and `?view=tips&tip=ID` params to open specific content.
- **Education area restructured**: Guides and Trading Tips as separate sidebar items, URL-driven via `useSearchParams`, no landing hub, calculated read times, level filter on Guides, improved reader modal with proper heading/paragraph rendering.
- **Server-side caching proxy**: `/api/quote` and `/api/timeseries` endpoints; `TWELVE_DATA_API_KEY` kept server-side only.
- **Demo seed data**: 90 trades Dec 2025–May 2026, £140k account, entry/exit times for session analysis.

## In Progress

## Backlog
